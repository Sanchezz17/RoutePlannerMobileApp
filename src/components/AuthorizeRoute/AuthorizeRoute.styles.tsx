import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    drawerContent: {
        height: '100%',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
    },
    signInButton: {
        height: '10%',
    },
});
