import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    view: {
        height: '100%',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
    },
    signInButton: {
        height: '10%',
    },
});
