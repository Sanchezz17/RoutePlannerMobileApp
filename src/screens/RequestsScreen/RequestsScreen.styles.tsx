import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    view: {},
    loadingText: {
        textAlign: 'center',
        margin: 15,
        fontSize: 17,
    },
    requests: {
        height: '100%',
    },
});
